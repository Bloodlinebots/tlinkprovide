{
  "name": "Hybrid TeraBox Bot",
  "description": "Telegram bot that provides unique TeraBox links using MongoDB and .txt file hybrid system.",
  "repository": "https://github.com/your-username/hybrid-terabox-bot",
  "logo": "https://i.imgur.com/8Pq0P4O.png",
  "keywords": ["telegram", "bot", "terabox", "pyrogram", "mongodb", "hybrid"],
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID from https://my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API HASH from https://my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Bot token from @BotFather",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB connection string (from MongoDB Atlas)",
      "required": true
    },
    "ADMIN_IDS": {
      "description": "Space-separated Telegram user IDs of admins",
      "required": true
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "addons": [],
  "stack": "heroku-22"
}
